{% extends '::backend.html.twig' %}

{% block id 'city' %}
{% block title %}Add a new city{% endblock %}

{% block article %}
<h1>{{ block('title') }}</h1>

<form action="{{ path('backend_city_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button class="button" type="submit">Add city</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_city') }}">
            Back to list
        </a>
    </li>
</ul>
{% endblock %}
