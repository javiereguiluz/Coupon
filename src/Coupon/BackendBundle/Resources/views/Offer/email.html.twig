<html>

<head>
    <style>
        body { font: 13px/1.5 Arial, sans-serif; }
        ul { margin-left: .5em; padding-left: 0; }
    </style>
</head>

<body>
    <table cellpadding="0" cellspacing="0" style="width:100%;" width="100%">
        <tr style="background: #00154E; color: #FFF;">
            <td style="padding: 1em;"><a style="color: #FFF; font-size: 21px; text-decoration: none;" href="{{ host ~ path('homepage') }}">Coupon</a></td>
            <td style="text-align: right; padding: 1em;">Daily offer at <span style="color: #FF3385; display: block; font-size: 18px;">{{ city.name }}</span></td>
        </tr>

        <tr>
            <td style="padding-top: 5px;">
                <a style="color: #000; font-size: 21px; font-weight: bold; text-decoration: none;" href="{{ url('offer', { 'city': offer.city.slug, 'slug' : offer.slug }) }}">{{ offer.name }}</a>
                <div style="font-size: 16px;">{{ offer.description | show_as_list }}</div>
            </td>
            <td style="padding-top: 20px;">
                <img style="margin: 1em 0; max-width: 250px; outline: 2px solid #CCC;" src="{{ host ~ images_dir ~ offer.photo }}" />
            </td>
        </tr>

        <tr>
            <td>
                <a style="background: #C06; border-radius: 5px; color: #FFF; font-size: 21px; padding: .5em 1em; text-decoration: none;" href="{{ host ~ path('offer', { 'city': offer.city.slug, 'slug' : offer.slug }) }}">View offer</a>
            </td>
            <td style="font-size: 28px; padding: .5em;">
                {{ offer.price }} &euro; <strong style="color: #C06; padding-left: 1em;">{{ discount(offer.price, offer.discount) }}</strong>
            </td>
        </tr>

        <tr>
            <td colspan="2" style="font-size: 18px; padding: 1em .5em;"><strong>Order soon {{ user.name }}!</strong> This offer expires on {{ offer.expiredAt|fecha('long', 'medium') }}</td>
        </tr>

        <tr>
            <td colspan="2" style="color: #AAA; font-size: 13px;">
                &copy; {{ 'now'|date('Y') }} - You have received this offer because you are subscribed to the <em>Daily Offer</em> newsletter. Visit <a href="{{ host ~ path('user_profile') }}">your profile</a> to unsubscribe.
            </td>
        </tr>
</body>

</html>
