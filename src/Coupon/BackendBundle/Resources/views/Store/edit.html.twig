{% extends '::backend.html.twig' %}

{% block id 'store' %}
{% block title %}Edit {{ entity }} store{% endblock %}

{% block article %}
<h1>{{ block('title') }}</h1>

<form action="{{ path('backend_store_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="button" type="submit">Save changes</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_store') }}">
            Back to list
        </a>
    </li>
    <li>
        <form action="{{ path('backend_store_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete store</button>
        </form>
    </li>
</ul>
{% endblock %}
